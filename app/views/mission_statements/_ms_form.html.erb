<%= form_with(model: @mission_statement, local: true) do |f| %>
  <div class="form-group">
    <%= f.text_area :content, class: "form-control", rows: 12 %>
    <%= f.submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>